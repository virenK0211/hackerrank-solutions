//challenge https://www.hackerrank.com/challenges/security-bijective-functions
#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n;/* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    cin>>n;
  
    int flag=1; 
    int ar[n];  
    for(int x=0;x<n;x++)
        cin>>ar[x];
    for(int i=0;i<n;i++)
    {   for(int j=i+1;j<n;j++)
    
    {   //cout<<ar[j];
        if(ar[i]==ar[j])
        { flag=0;
       //cout<<flag;
        }
    }
        
    
        
    }
    if(flag==1)
        cout<<"YES"; else cout<<"NO";
    
    return 0;
}
